#include<stdio.h>
int prime(int n)
{
    int count=0;
    for(int i=1;i<=n/2;i++)
    {
        if(n%i==0)
        count++;
    }
    if(count==)
}
int main()
{
    int n,i;
    printf("enter the number: ");
    scanf("%d",&n);
    if (prime(n)&&prime(n-i))
    {
        printf("%d+%d",i,n-i);
    }
    return 0;
}
