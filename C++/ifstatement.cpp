#include<iostream>
int main()
{
    int age;
    std::cout<<"enter your age: ";
    std::cin>>age;
    if(age>=18)
    {
        std::cout<<"welcome to the site\n";
    }
    else if(age<0)
    else
    std::cout<<"get old enough bro!\n";
    return 0;
}
